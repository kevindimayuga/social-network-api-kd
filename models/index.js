// This will import the User model and the Thought model
const User = require('./User');
const Thought = require('./Thought');

// This will export the User model and the Thought model
module.exports = { User, Thought };